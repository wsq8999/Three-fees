# real: use the Java API (optionally through the Vite proxy); mock: use local development fixtures.
VITE_API_MODE=real
VITE_API_BASE_URL=
VITE_API_PROXY_TARGET=http://127.0.0.1:8080
VITE_API_TIMEOUT_MS=10000
