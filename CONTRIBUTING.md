# 参与开发

## 开始前

1. 阅读 `docs/PROJECT_MANUAL.md` 和与任务相关的 ADR/规范。
2. 明确当前变更属于哪个业务特性和固定 Agent 角色。
3. 写下可观察的验收条件；行为变更先写失败测试。
4. 确认工作区已有变更的所有权，不覆盖无关修改。

## 实施顺序

1. RED：契约/测试/验收检查先失败或确认尚未满足。
2. GREEN：只实现让条件通过的最小完整代码。
3. REFACTOR：消除重复、明确命名和边界，但不改变行为。
4. VERIFY：执行受影响子工程的全套验证，再执行仓库检查。
5. MEMORY：更新相关角色报告；架构、接口或数据模型变化时同步项目手册。

## 代码审查清单

- API 是否遵循 `docs/standards/API_STYLE.md`，状态码和 Problem Details 是否正确。
- Java 是否遵循业务特性分包、构造器注入、显式映射和事务边界。
- 前端是否 TypeScript strict、无会话令牌本地存储、具备加载/空/错误/无权限状态。
- 数据访问是否包含角色和地市范围，SQL 是否参数化且索引支持查询。
- 上传、AI 和日志是否避免敏感信息泄漏。
- 慢任务是否持久化、可幂等、可恢复，而不是进程内“后台跑一下”。
- 是否引入了未批准的付费服务或过早的分布式组件。
- 新行为是否有测试，验证结果是否真实记录。

## 禁止提交

- `.env`、真实口令/令牌/API Key、数据库连接凭据。
- AI DEMO 中的业务材料、上传文件、数据库、虚拟环境、`node_modules` 或构建产物。
- 未脱敏日志、导出文件、正式报告或生产备份。
- 仅在个人 IDE 生效、没有项目级配置的隐式规则。

